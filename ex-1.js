
let words = ["hello", "world"];

function getWordLengths(words) {
  // Start coding here
  return words.length;
}

const wordLengths = words.map(getWordLengths);

console.log(wordLengths); // Output: [5, 5]
